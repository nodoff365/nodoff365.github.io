## 실습 환경

| 호스트      | 네트워크 어댑터       | IP                          | 게이트웨이       |
| -------- | -------------- | --------------------------- | ----------- |
| rocky9-1 | 추가 (Host-only) | 10.0.0.11<br>172.16.0.11/24 | -           |
| rocky9-2 | Host-only 변경   | 172.16.0.21/24              | 172.16.0.11 |
| rocky9-3 | Host-only 변경   | 172.16.0.22/24              | 172.16.0.11 |
| rocky9-4 | 추가 (Host-only) | 10.0.0.14<br>172.16.0.12/24 | -           |
```bash
# 물리 pc cmd
ssh-keygen -t rsa -b 2048 -m PEM -q -N ""
#id_rsa, id_rsa.pub 생성됨

scp .ssh\id_rsa.pub root@10.0.0.11:/root/.ssh/authorized_keys #공개키 전송
ssh root@10.0.0.11 # pw 묻지 않고 바로 ssh 접속

scp .ssh\id_rsa root@10.0.0.11:/root/.ssh/ #개인키 전송 
---------------------------------------------------------------
#추가정리필요
scp .ssh/sgm.pub root@10.0.0.11:/root/.ssh/authorized_keys
sgm.pub

ssh -i .ssh\sgm root@10.0.0.11

fsutil file createnew a.txt 1024
scp -i .ssh/sgm a.txt root@10.0.0.11:/root

scp -i .ssh\sgm .ssh\id_rsa.pub root@10.0.0.11:/root/.ssh/authorized_keys

ssh root@10.0.0.11

scp .ssh\id_rsa root@10.0.0.11:/root/.ssh/
```

```bash
# rocky9-1 #추가정리필요
vi /etc/ssh/sshd_config
49 # authorized_keys 확인
```

```bash
# rocky9-1
scp .ssh/authorized_keys root@172.16.0.21:/root/.ssh/
scp .ssh/authorized_keys root@172.16.0.22:/root/.ssh/
chmod 600 .ssh/id_rsa

ssh root@172.16.0.21 # passwd 없이 접속 되는지 확인 
ssh root@172.16.0.22

echo "net.ipv4.ip_forward = 1" > /etc/sysctl.d/ip_forward.conf
firewall-cmd --permanent --direct --add-rule ipv4 nat POSTROUTING 0 -o ens160 -j MASQUERADE
firewall-cmd --permanent --direct --add-rule ipv4 filter FORWARD 0 -i ens192 -o ens160 -j ACCEPT
firewall-cmd --reload
shutdown -r now # systemctl restart NetworkManager
# 설정 후 rocky9-2~3 에서 외부로 ping 나가는지 확인
```

```bash
# rocky9-2
dnf install -y httpd

echo $HOSTNAME > /var/www/html/index.html
vi /etc/httpd/conf/httpd.conf
47 60080 # 수정
systemctl enable --now httpd

firewall-cmd --permanent --add-port=60080/tcp
firewall-cmd --reload
```

```bash
# rocky9-3
dnf install -y httpd

echo $HOSTNAME > /var/www/html/index.html
vi /etc/httpd/conf/httpd.conf
47 60080 # 수정
# sed -i "s/Listen 80/Listen 60080/g" /etc/httpd/conf/httpd.conf
systemctl enable --now httpd

firewall-cmd --permanent --add-port=60080/tcp
firewall-cmd --reload
```

```bash
# rocky9-4
[root@rocky9-4 ~]# curl 172.16.0.22:60080
rocky9-3
[root@rocky9-4 ~]# curl 172.16.0.21:60080
rocky9-2

dnf install -y haproxy
vi /etc/haproxy/haproxy.cfg
68 bind *:5000 # 5000 -> 80
72 use_backend static # static -> app
87 172.16.0.21:60080
88 172.16.0.22:60080
89 #주석처리 
90 #주석처리 

systemctl enable --now haproxy
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --reload
```